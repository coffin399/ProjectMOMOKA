# Project MOMOKA requirements (legacy local pipeline)
--extra-index-url https://download.pytorch.org/whl/cu118

discord.py
discord.py[voice]
aiohttp
openai
pyyaml
google-genai
PyNaCl
yt-dlp
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
aiofiles
matplotlib
japanize-matplotlib
cartopy
langdetect
pillow

# Local image generation pipeline
# Pin NumPy to 1.x for compatibility with torch 2.1.0
numpy<2.0.0


# Style-Bert-VITS2 TTS engine dependencies
# Note: The Style-Bert-VITS2 source code is integrated into this project.
# See NOTICE file and MOMOKA/generator/tts/LICENSE_SBVITS2 for details.
loguru>=0.7.0  # For logging (used by integrated style_bert_vits2 package)
pydantic>=2.0.0  # For data validation (used by TTSModel)
pyworld>=0.3.0  # For voice processing (f0 extraction and synthesis)
pyannote.audio>=3.0.0  # For style vector generation (wespeaker-voxceleb-resnet34-LM)
sentencepiece>=0.1.99  # For BERT tokenization
onnxruntime>=1.15.0  # Optional: for ONNX inference acceleration
numba>=0.56.0  # For JIT compilation (used in monotonic_alignment.py)
fastapi>=0.100.0  # For HTTP exception handling (used in user_dict module)
num2words>=0.5.11  # For Japanese number normalization
inflect>=6.0.0  # For English number normalization
g2p-en>=2.1.0  # For English grapheme-to-phoneme conversion
jieba>=0.42.1  # For Chinese word segmentation
pypinyin>=0.49.0  # For Chinese pinyin conversion
cn2an>=0.5.17  # For Chinese number normalization
pyopenjtalk-plus==0.4.1.post4
pyopenjtalk-plus[onnxruntime]

# PyTorch related (CUDA 11.8)
torch==2.1.0+cu118
torchvision==0.16.0+cu118
torchaudio==2.1.0+cu118
xformers==0.0.22.post7

# Image generation pipeline (versions compatible with torch 2.1.0)
diffusers==0.23.0
transformers==4.35.2
accelerate==0.25.0
safetensors==0.4.1
huggingface-hub==0.19.4
omegaconf>=2.2.0

# For release for those who find self-hosting troublesome
nuitka

# For GUI Log Viewer
tk
pillow  # Already exists, but specified just in case
ttkthemes
darkdetect>=0.8.0