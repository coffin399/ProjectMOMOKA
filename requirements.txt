# Project MOMOKA requirements
# Requires Python 3.11.x (see startMOMOKA.bat / main.py version guard)
#
# ML stack note:
#   Style-Bert-VITS2 upstream targets torch<2.4. Keep torch 2.1.0+cu118
#   + matching torchvision/torchaudio/xformers/diffusers pins together.
#   Do not bump torch alone.
--extra-index-url https://download.pytorch.org/whl/cu118

# --- Discord / core ---
# discord.py 2.7+: native DAVE voice; install davey or voice may fail with close 4017.
discord.py[voice]>=2.7.0
davey>=0.1.0
aiohttp>=3.9.0
ddgs>=9.0.0
openai>=1.40.0
pyyaml>=6.0
google-genai
PyNaCl>=1.5.0
yt-dlp[default]>=2026.3.17
# yt-dlp[default] に yt-dlp-ejs が含まれる（YouTube JS チャレンジ解決に必須）
# 加えて OS に Deno または Node.js 22+ が PATH 上にあること（推奨: Deno）
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
aiofiles>=23.0.0
matplotlib
japanize-matplotlib
cartopy
langdetect
pillow>=10.0.0

# --- NumPy (1.x required by torch 2.1 / numba / pyopenjtalk) ---
numpy>=1.24.0,<2.0.0

# --- Style-Bert-VITS2 TTS (integrated under MOMOKA/generator/tts) ---
# See NOTICE and MOMOKA/generator/tts/LICENSE_SBVITS2 for details.
loguru>=0.7.0
pydantic>=2.0.0
pyworld>=0.3.0
# pyannote 4.x requires torch>=2.8 — keep 3.x while pinned to torch 2.1.0
pyannote.audio>=3.0.0,<4
sentencepiece>=0.1.99
onnxruntime>=1.15.0
# numba 0.56.x does NOT support Python 3.11
numba>=0.57.0
fastapi>=0.100.0
num2words>=0.5.11
inflect>=6.0.0
g2p-en>=2.1.0
jieba>=0.42.1
pypinyin>=0.49.0
cn2an>=0.5.17
pyopenjtalk-plus==0.4.1.post8
pyopenjtalk-plus[onnxruntime]

# --- PyTorch (CUDA 11.8, Python 3.11) ---
# cu118 wheels: Linux/Windows only. macOS CI uses CPU torch (see workflow).
torch==2.1.0+cu118
torchvision==0.16.0+cu118
torchaudio==2.1.0+cu118
xformers==0.0.22.post7

# --- Image generation (locked to torch 2.1.0 generation) ---
diffusers==0.23.0
transformers==4.35.2
accelerate==0.25.0
safetensors==0.4.1
huggingface-hub==0.19.4
omegaconf>=2.2.0

# --- Release builds (pin matches .github/workflows/build_and_release.yml) ---
nuitka==1.9.7

# --- GUI log viewer (tkinter is stdlib) ---
ttkthemes
darkdetect>=0.8.0
